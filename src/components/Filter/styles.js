import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  filter: {
    display: "flex",
    margin: "30px auto",
    justifyContent: "center"
  },
  field: {
    margin: "0 50px",
    width: "250px"
  }
});
